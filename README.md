# NotesDeCours
Notes de cours

## To run locally
```zsh
pnpm next
```

## To build
```zsh
pnpm next build
```
This will create produce an `out` folder which contains the HTML/CSS/JS assets for your application.